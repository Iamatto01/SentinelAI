{
  "name": "sentinelai",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently --kill-others-on-fail -n \"backend,frontend\" -c \"blue,green\" \"npm run dev --prefix backend\" \"sleep 3 && npm run dev --prefix frontend\"",
    "build": "npm run build --prefix frontend",
    "backend": "npm run dev --prefix backend",
    "frontend": "npm run dev --prefix frontend",
    "install:all": "npm install --prefix backend && npm install --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  },
  "dependencies": {
    "clsx": "^2.1.1",
    "motion": "^12.38.0",
    "tailwind-merge": "^3.5.0"
  }
}
